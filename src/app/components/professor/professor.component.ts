import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * The component for the Professor dashboard.
 *
 * @remarks
 * This component is intended for professors to manage their courses, students, and grades.
 * (Currently implemented as a placeholder structure).
 */
@Component({
  selector: 'app-professor',
  standalone: true,
  imports: [CommonModule],
  templateUrl: './professor.html',
  styleUrls: ['./professor.css'],
})
export class ProfessorComponent{}
